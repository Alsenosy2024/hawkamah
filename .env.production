# Production build settings (committed; contains no secrets).
# The Governance Copilot calls the Python google-genai agent on Cloud Run
# directly (CORS-allowlisted to hawkamah.web.app). Direct calls (not the Firebase
# Hosting rewrite) are required because long document drafts exceed Hosting's
# 60s rewrite timeout; Cloud Run allows up to several minutes.
VITE_COPILOT_API=https://hawkama-copilot-xuy3xd24hq-uc.a.run.app/copilot
