# Production build settings (committed; contains no secrets).
# Routes the Governance Copilot to the Python google-genai agent on Cloud Run,
# served same-origin via the Firebase Hosting rewrite (/copilot/** → Cloud Run).
VITE_COPILOT_API=/copilot
